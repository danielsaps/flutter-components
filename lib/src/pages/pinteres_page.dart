import 'package:flutter/material.dart';

class PinterestPage extends StatelessWidget {
  static final String routeName = 'pinterest';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('PinterestPage'),
      ),
      body: Center(
        child: Text('PinterestPage'),
      ),
    );
  }
}
